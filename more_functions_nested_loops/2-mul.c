#include "main.h"
/**
 * mul - checks for  digit (0 through 9).
 *
 * @a: is the varible to check it
 * @b: the variable to check
 * Return: 1 if digit else 0.
 */
int mul(int a, int b)
{
	return (a * b);
}

